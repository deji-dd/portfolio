sync:
  defaults:
    mode: "two-way-resolved"
    ignore:
      paths:
        - "node_modules"
        - ".next"
        - ".git"
        - ".DS_Store"
  portfolio-sync:
    alpha: "."
    beta: "deji@100.99.211.68:/home/deji/repos/portfolio"

# Custom commands for automation
commands:
  dev: ssh -t deji@100.99.211.68 "cd /home/deji/repos/portfolio && pnpm dev"
